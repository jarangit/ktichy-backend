meta {
  name: Update Product
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/products/{{productId}}
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "เนื้อย่าง",
    "storeId": "{{storeId}}",
    "stationId": "{{stationId}}",
    "categoryId": "{{categoryId}}"
  }
}
