meta {
  name: Get Store By User Id
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/stores/user/{{userId}}
  body: none
  auth: bearer
}

headers {
  Authorization: Bearer {{token}}
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  userId: 1
}
